
Based on Verse.ThingDef.DescriptionDetailed (Line 426)


//public string TagFriendlyName
        //{
        //    get
        //    {
        //        if (tagNameCached == null)
        //        {
        //            StringBuilder stringBuilder = new StringBuilder();
        //            stringBuilder.Append(label);
        //            if (IsTag)
        //            {
        //                stringBuilder.AppendLine(string.Format("TEST"));
        //            }
        //            tagNameCached = stringBuilder.ToString();
        //        }
        //        return tagNameCached;
        //    }
        //}
        /* 
         * This (above) may be where I need to focus. The "AppendLine" should reference a private string that is received when 
         * the "OK" button is clicked on the rename dialog.
         */
        //public bool IsTag
        //{
        //    get
        //    {
        //        return isTag != null;
        //    }
        //}
        /*
        * based on Vanilla ThingDef IsApparel Bool (line 483). 
        * Will this be called on when pushing the button. 
        * So like Button A pressed, window appears, window contains "isTag = TagA"?
        * Window Confirms Input and pushes it to ItemTagDef?
        */
        //public TagProperties isTag; //based on Vanilla ThingDef apparel Field (line 2488)
        //[Unsaved(false)]
        //private string tagNameCached; //vanilla ThingDef (line 2519)